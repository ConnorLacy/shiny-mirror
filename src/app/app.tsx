/*
    This is the React application we will be rendering on the server
    and serving to the client as a string -- then binding the react/js
    to the dom elements
*/

import React from 'react'

export const App = () => {
  return <h1>This is the app!</h1>
}
